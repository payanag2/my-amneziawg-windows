# MyAmneziaWG for Windows

This is the standalone MyAmneziaWG Windows client fork.

The standalone build uses separate application, manager, tunnel, configuration, and installer identities so it can coexist with the original AmneziaWG Windows client.

## Build

GitHub Actions builds x86, amd64, and arm64 MSI packages.
